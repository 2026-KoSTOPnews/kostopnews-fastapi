services:
  fastapi:
    build: .
    container_name: fastapi
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - mlops-network

networks:
  mlops-network:
    external: true